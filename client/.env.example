# Vercel Production Environment Variables (Template)
# Rename this file to .env.local for local development, or set these directly in Vercel.

# Must exactly match the Render backend URL in production
REACT_APP_API_URL=https://gatepedia-api.onrender.com/api
